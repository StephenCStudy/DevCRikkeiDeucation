#include <stdio.h>;

 int main(){
 	int N;
 	printf("Moi nhap vao mot so nguyen:");
 	scanf("%d", &N);
 	if (N>=1){
 		printf("so nay la so duong");
	 }
	 else {
	 	printf("so nay la so am");
	 }
 	return 0;
 }