#include <stdio.h>;

 int main(){
 	int N;
 	printf("Moi nhap vao mot so nguyen:");
 	scanf("%d", &N);
 	if (N % 2 == 0){
 		printf("so nay la chan");
	 }
	 else {
	 	printf("so nay la le");
	 }
 	return 0;
 }