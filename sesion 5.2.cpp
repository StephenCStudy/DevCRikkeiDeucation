#include <stdio.h>
 
  int main (){
  	int a = 5;
  		while (true){
		  	  
  		printf("nhap vao so (a) cho truoc (1-9): \n");
  	    scanf("%d", &a); 
  	   if ( a == 5){
  	   	 printf(" so (%d) la so cho truoc: \n",a);
  	   	 break;
		 }else {
		 }
		 printf(" so (%d) khon phai la so cho truoc: \n \n",a);
	    }  
	    
  	return 0;
  }